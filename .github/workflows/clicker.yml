name: '🤖 Автоматический Кликер Профилей 🎯'

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  run-clicker:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Извлечение репозитория
        uses: actions/checkout@v4

      - name: Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install aiohttp

      - name: Запуск прокликивания страниц
        run: python clicker/clicker.py

      - name: Фиксация и отправка изменений
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add clicker/extracted_urls.txt
          
          # Если изменения есть — стягиваем новые базы от сортировщика через rebase и плавно пушим
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Кликер: Обновлен список извлеченных URL" && git pull --rebase origin main && git push)
